#include <stdio.h>
/**
 * main - prints all single digits numbers of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char rev;

	for (rev = z; rev <= a; rev--)
	{
		putchar(rev);
	}
	putchar('\n');
	return (0);
}
