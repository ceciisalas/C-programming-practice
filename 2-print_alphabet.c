#include <stdio.h>
#include <stdlih.h>
/*
 * main: Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char a, z;

	for (a = 97; a <= 122; a++)
	{
		putchar (a);
	}
	putchar('\n');
	return (0);
}
